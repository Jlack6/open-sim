#!/usr/bin/env bash
# Ensure new commits use GitHub's private noreply email (email is set at commit time).
ROOT="$(git rev-parse --show-toplevel)"
exec "$ROOT/scripts/use-private-git-email.sh" --quiet
